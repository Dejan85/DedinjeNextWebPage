# Project status — Institut Dedinje

**Poslednje ažuriranje:** 2026-08-05 (header logo podnaslov promenjen sa "Институт за КВБ" na "Национални институт", `components/shared/Header/Header.tsx:202`, po anotiranom screenshotu vlasnika sajta — vidi Dnevnik ispod; ranije istog dana homepage "Шта кажу наши пацијенти" — ugrađen Hipokratija widget (prave ocene pacijenata) na `sr` lokalu, zamenjuje demo-seed kartice — vidi Dnevnik ispod; ranije istog dana header kontakt traka — uklonjene telefon/@/pin ikonice; header ponašanje pri skrolu prošlo kroz 3 iteracije u istom tasku (fixed→static→fixed→**finalno `position: absolute`**) dok se nije razjasnilo da vlasnik sajta hoće da header vizuelno izgleda isto kao sad ali da se fizički skroluje zajedno sa stranicom — vidi Dnevnik ispod; ranije istog dana homepage "Наш тим" sekcija — 4 demo lekara zamenjena pravim lekarima Instituta (prave fotografije, vlasnik sajta dostavio), uklonjene ikonice društvenih mreža sa kartica lekara — vidi Dnevnik ispod; ranije istog dana homepage "Најновије вести"/"Гостовања у медијима" sekcija dinamički isključena + iste vesti sad prikazane u novom vidžetu odmah ispod "Предстојећи догађаји" — vidi Dnevnik ispod; ranije istog dana WordPress migracija /aktuelnosti/vesti — 302 od 305 članaka uneseno sa fotografijama, pagination i sorting dodani, stari 6 placeholder dokumenata obrisani — vidi Dnevnik ispod; ranije istog dana nova homepage sekcija "Наши партнери" — demo logo-grid 9 partnerskih klinika/institucija odmah ispod "Наше клинике", logotipi preuzeti sa zvaničnih sajtova — vidi Dnevnik ispod; ranije istog dana sekcija "Наше клинике" proširena sa 6 na svih 13 klinika/službi po specifikaciji vlasnika sajta, sve postojeće rute; ranije istog dana 4 sekcije privremeno isključene na zahtev vlasnika sajta — Usluge/"Zašto mi"/citat direktora+CTA baner/"Kontaktirajte nas"+mapa — preko dinamičkog toggle-a u kodu, ništa obrisano; ranije istog dana nov `event` Sanity tip za događaje, homepage "Добродошли" sekcija sad prikazuje "Предстојећи догађаји" vidžet kad ih ima, `bodyText` proširen punim tekstom koji je vlasnik sajta dostavio, info kartice redizajnirane sa novim `contact` variant tipom, nova `/za-pacijente` landing stranica, nova `/nas-tim` stranica — vidi Dnevnik ispod)
**Stanje:** Sajt je funkcionalan i deploy-ovan na Vercel; Sanity migracija je suštinski završena. **Aktivan task: puna SRP/ENG i18n infrastruktura** — **Faze 3a/3b/3c/3d/3e ✅ gotove.** Faza 3d prevela svih 2742 lokalizovanih polja (99 dokumenata) i upisala ih u `production`; `enReviewed: boolean` gate (po dokumentu) dodat na 20 schema tipova; Faza 3e nošivela sve 83 fetch call-site-a kroz `localize()` helper; UI prekidač za jezik (`LanguageSwitch.tsx`) zamenjuje stare `LocaleSwitch`/`ScriptToggle`, nudi 3 opcije (English, Српски ћирилица, Srpski latinica). **Četiri kritična bag-fixa u istoj sesiji:** (1) Header/Footer su se rendivali iz root `app/layout.tsx` i nisu se ažurivali između `/` i `/en` — premešteni u `app/[locale]/layout.tsx`; (2) middleware kolačići (`localeDetection: true`) su sprečavali soft navigaciju — ispravljeno na `localeDetection: false`; (3) globalni transliteration engine je greškom menjao tekst `LanguageSwitch` labela i `StatCounter` brojki — dodat `data-no-translit` atribut na oba mesta; (4) **svi interni linkovi na sajtu** (nav meni, footer, kartice, "pročitaj više" dugmad — 15 fajlova) koristili su obično `next/link` sa fiksnim putanjama umesto next-intl-ovog locale-svesnog `Link`-a iz `@/i18n/navigation` — posledica: svaki klik na navigaciju dok si na `/en/*` vraćao te na srpsku (bez-prefiksnu) verziju te iste stranice, gubeći `/en`. Svih 15 fajlova prebačeno na `import { Link } from "@/i18n/navigation"` (samo `LanguageSwitch.tsx` namerno zadržava plain `next/link`, jer on svesno gradi cross-locale hreference). **Sledeće: Faza 3f — vlasnik sajta pregleda prevode u Studio-u i čekira `enReviewed` po dokumentu**, tek onda se sitemap gate uklanja. Vidi [`TASKS.md`](TASKS.md) za tačan status. Ispod su blokeri i odluke.

---

## Blokeri / Odluke

- **`ThemeColorSwitcher` namerno vidljiv na produkciji** (`app/layout.tsx`) — normalno je bio ograničen na `NODE_ENV !== "production"`, privremeno uklonjen taj uslov na eksplicitan zahtev vlasnika sajta (2026-07-23) dok se sajt još doteruje. **Ukloniti pre pravog lansiranja sajta** — vratiti `{process.env.NODE_ENV !== "production" && <ThemeColorSwitcher />}` ili potpuno skloniti komponentu.
- **Kontakt forma ne šalje nikuda** (`app/kontakt/page.tsx`, forma sada u `KontaktForm.tsx`) — `handleSubmit` samo poziva `e.preventDefault()` i prikazuje lažnu poruku o uspehu; nema `/api` rute, nema email servisa (Resend/Nodemailer/itd.). Sadržaj stranice (kontakt kartice) je od 2026-08-03 Sanity-backed (`siteSettings`), ovo je isključivo infra pitanje slanja emaila. Sanity `contactSection` schema (homepage) već ima polja za punu booking formu, ali se nigde ne koristi. Treba pre lansiranja odlučiti email servis i povezati.
- **5 mrtvih linkova u meniju** ka nepostojećim stranicama: `/nauka-istrazivanje/korisni-linkovi/{nitra,amprec,kobson,zajednica-instituta}` i `/nauka-istrazivanje/monografija` (definisani u `navigation` Sanity singletonu / `Header.tsx` fallback-u, stranice ne postoje → 404).
- **Nema testova ni CI/CD** — ako se ovo doda, prvo definisati da li ima smisla za pretežno marketinški/informativni sajt ili samo build+lint gate na push.
- **`useCdn: false`** u `sanity/lib/client.ts` — svaki zahtev ide direktno na Sanity API (sveži sadržaj odmah posle izmene u Studio-u, ali sporije i veća potrošnja API kvote). Namerno tako zbog uređivačke svežine — razmotriti CDN + revalidaciju ako saobraćaj poraste.
- **`npm run build:static` fundamentalno ne radi** — dinamički (`revalidate:0`) Sanity fetch na skoro svakoj stranici je nekompatibilan sa `output: "export"` (nema servera za fetch na zahtev). Otkriveno 2026-08-04 tokom i18n rutiranja; detalji i predlog rešenja u `TASKS.md` Backlogu. Vercel (`npm run build`) radi ispravno — ovo utiče samo na alternativni static-hosting put.
- **Hardkodovani Sanity `projectId`/`dataset`** na više mesta (`sanity.config.ts`/`sanity.cli.ts`/`sanity/lib/client.ts` hardkoduju `"haygvfxq"`/`"production"` umesto da čitaju env svuda) — rizik samo ako se ikad pravi drugi Sanity projekat/dataset (npr. staging).
- **`public/doctor-milan-nikolic.png`** — orphan asset na root nivou `public/`, nigde referenciran u kodu; ili ga povezati sa pravim doktorom ili obrisati.
- **`/aktuelnosti/casopis-dedinje` i `/aktuelnosti/informator` prikazuju netačne informacije** — oba linkuju na `/pdf/АКАДЕМИК-CV-АВГУСТ-2025.pdf` (CV direktora) umesto na prave dokumente; časopis dodatno ima izmišljene naslove/teme po izdanju. Pravi arhiv časopisa na starom sajtu ima 27 sekvencijalnih brojeva. Od 2026-08-03 ovaj (netačan) sadržaj je na Sanity-ju (`magazineIssue` dokumenti, `informatorPage` singleton) — **ispravka je sad Studio-edit, ne code change**, ali i dalje treba pravi PDF materijal od vlasnika sajta.
- **`/klinike/kardiohirurgija` stat "95,5% Стопа преживљавања" bez potvrđenog izvora** i `/za-pacijente/preoperativna-priprema` moguće nedostajući lek "Хсердоксо" — otkriveno tokom docx-audit sweep-a 2026-07-21, detalji u `TASKS.md` backlogu i `popunjene-stranice-2026-07-21.md`.
- **`event` dokumenti (homepage "Предстојећи догађаји" vidžet) imaju placeholder datume** — 4 primer događaja seed-ovana skriptom `scripts/migrate-events.ts` (Годишњи конгрес Института 2027-04-15, Сретењска радионица 2027-02-13, Конгрес кардиоваскуларне превенције 2027-06-10, Школа ехокардиографије нови циклус 2026-09-14) su procena "sledećeg termina" na osnovu istorijskog obrasca pominjanog u drugom sadržaju na sajtu (4. dodat 2026-08-05 na zahtev vlasnika sajta da vidžet prikazuje 4 umesto 3 stavke, datum procenjen na osnovu obrasca "2 kursa godišnje" sa postojeće `/edukacija/programi/skola-ehokardiografije-...` stranice — 33. kurs završen kraj maja 2026) — **nisu potvrđeni od vlasnika sajta**. Treba potvrditi prave datume (i po potrebi dodati/ukloniti događaje) kroz Studio.
- **Opšta klasa rizika: transliteration engine (`transliterateDom.ts`) vs. dinamički client-side tekst** — otkriveno 2026-08-04 na `StatCounter.tsx` (brojač zaglavljen na "0" na sr, vidi dnevnik) i `LanguageSwitch.tsx` labelama. Bilo koja komponenta koja menja svoj TEKST posle mount-a preko `setState` (ne samo props od servera) unutar `ScriptProvider` stabla je podložna istom bag-u (WeakMap keš "originalnog" teksta + MutationObserver prepisuju React-ov update nazad). Ispravka je uvek ista: `data-no-translit` na taj element. `HeroSection.tsx`/`HeroSlider.tsx` koriste `requestAnimationFrame`/`setInterval` (za slajd tranzicije) — nisu provereni da li imaju isti problem, vredi proveriti ako se primeti da tekst slajdova "zamrzava" na srpskoj verziji.
- **Homepage "Наши партнери" logotipi nisu formalno licencirani** (`public/images/partners/`, dodato 2026-08-05) — preuzeti direktno sa zvaničnih sajtova institucija (curl) na eksplicitan zahtev vlasnika sajta, isključivo za demo prikaz. Fuwai Hospital nema logo fajl (izvorni sajt nedostupan sa naše mreže) — prikazuje tekstualni fallback. **Pre pravog lansiranja:** vlasnik sajta treba da potvrdi/pribavi zvanično odobrenje ili zamenu materijala za svih 9 partnera (i dopuni Fuwai logo ako ga obezbedi).
- **Homepage testimonialsSection — demo-seed zamenjen pravim ocenama SAMO na `sr` lokalu** (`scripts/migrate-all.ts` i dalje seed-uje izmišljene pacijente sa stock fotografijama, ali se od 2026-08-05 ne prikazuju na `sr` — vidi dnevnik, Hipokratija widget). Na `/en` i dalje prikazuje stari demo-seed (fake pacijenti, prevedeni) jer Hipokratija widget nema EN varijantu — **treba odluka vlasnika sajta** da li je to prihvatljivo trajno stanje ili treba drugo rešenje za englesku verziju. (`teamSection` ISPRAVLJENO 2026-08-05 — sad prikazuje 4 prava lekara Instituta; `statsSection` ISPRAVLJENO 2026-08-05 — realne brojke, vidi dnevnik.) Hero (od 2026-08-04 jedan statičan video, ranije 4-slajd slajder) i "Наше клинике"/"За пацијенте" brzi linkovi su tačan sadržaj na Sanity-ju (vidi dnevnik). Detalji u `TASKS.md` backlogu i `popunjene-stranice-2026-07-21.md` → "Audit početne strane". (Footer kontakt telefon/email je ispravljen 2026-07-21, vidi dnevnik.)
- **Hipokratija widget je eksterna zavisnost bez SRI-ja** (`https://hipokratija-widget.vercel.app/assets/index.js`, učitan preko `next/script` u homepage testimonials sekciji) — treća strana kontroliše kartice/ocene koje se prikazuju na sajtu; ako njihova infrastruktura padne ili promeni ponašanje, utiče direktno na naš sajt. `HOMEPAGE_SECTION_TOGGLES.hipokratijaWidget` u `app/[locale]/page.tsx` omogućava brzo gašenje (vraća stari demo-seed grid) bez code rollback-a.
- **Prave adrese/mape za Дедиње 2 i Дедиње 3 zgrade (footer)** — novo polje `locations[]` u `footer` Sanity singletonu (dodato 2026-08-05 tokom redizajna) ima privremeno iste adrese kao Дедиње 1 (Хероја Милана Тепића бр. 1, 11040 Београд) i placeholder Google Maps iframe-ove. Vlasnik sajta treba da dostavi tačne adrese i GPS koordinate za sve tri zgrade kako bi se mape i tekstualne adrese ispravile kroz Sanity Studio (bez potrebe za code change).

## Dnevnik

> Dodaj red ovde na kraju svakog taska koji menja sadržaj/arhitekturu — šta je urađeno, kako, watch-item ako ima.

- 2026-08-05 — **Header logo podnaslov — "Институт за КВБ" → "Национални институт".** Vlasnik sajta poslao anotirani screenshot header logoa (crveni okvir oko podnaslova ispod "ДЕДИЊЕ") sa tekstom "Ovde može da ide tekst **Nacionalni Institut**". `components/shared/Header/Header.tsx:202` — hardkodovan `<span className={styles.logoSubtitle}>` tekst promenjen (nije Sanity-backed, statičan string u komponenti, isti kao pre). Sad se poklapa sa formulacijom iz hero naslova ("Национални институт за срце и крвне судове"). Nema fiksne širine/`nowrap` na `.logoSubtitle` (CSS) pa duži tekst ne lomi layout — potvrđeno Playwright screenshotom. **Napomena:** ovaj tekst je i dalje uvek na ćirilici bez obzira na `/en` lokal (pre-postojeći, ovim taskom nedirano — logo blok nema locale-svesnu granu koda).
- 2026-08-05 — **Homepage "Шта кажу наши пацијенти" — ugrađen Hipokratija widget (prave ocene pacijenata sa treće strane).** Vlasnik sajta dostavio `public/pdf/Widget - tehnicki dokument.pdf` (uputstvo platforme Hipokratija za embed ocena/iskustava pacijenata) i tražio da se widget prikaže direktno u postojećoj sekciji, ne kao spoljni link. Rešenje: `app/[locale]/page.tsx` — novi `HOMEPAGE_SECTION_TOGGLES.hipokratijaWidget` flag (default `true`); kad je uključeno i `locale === "sr"`, testimonials sekcija renderuje `<div class="hipokratija-widget" data-entity_slug="institut-za-kardiovaskularne-bolesti-dedinje">` + `next/script` (`type="module"`, `strategy="afterInteractive"`) koji učitava `https://hipokratija-widget.vercel.app/assets/index.js`; postojeći Sanity demo-seed kartice + dekorativna ‹ › navigacija (widget ima sopstvenu) ostaju kao fallback kad je toggle isključen ili na `/en` (gde widget nema prevod). Sekcijski okvir (crveni/plavi gradijent, bedž, naslov) je nepromenjen — i dalje iz Sanity `testimonialsSection` bloka. **CSS fix (`page.module.css`):** widget-ove review kartice (`.glide__slide`) imaju providnu pozadinu + sivi tekst dizajniran za belu stranicu — na gradient pozadini sekcije bio je nečitljiv; dodat `:global()` override (`background-color: var(--white)`, `color: var(--gray-900)` na `.content`) — bezbedno jer widget nije u Shadow DOM-u (obična global CSS klasa, potvrđeno inspekcijom DOM-a). Verifikovano uživo (Playwright na postojećem dev serveru): widget učitava STVARNE podatke (4.7 prosečna ocena, 27.759 iskustava, pravi profil link `hipokratija.rs/ustanove/institut-za-kardiovaskularne-bolesti-dedinje`), 0 console/page grešaka, kartice čitljive posle CSS fix-a, `/en` ispravno prikazuje stari demo-seed fallback bez widget-a, soft-navigacija (klijentska ruta → povratak na `/`) ispravno re-renderuje widget. `npx tsc --noEmit` čisto, `npm run lint` 28 problema (baseline nepromenjen, nula novih u dirnutim fajlovima). **Watch-item:** widget je eksterna `*.vercel.app` zavisnost bez SRI-ja (vidi Blokeri iznad) i nema EN sadržaj — `/en` i dalje prikazuje demo-seed fake pacijente dok se ne odluči trajno rešenje za englesku verziju.

  **Dopuna istog dana — prioritizacija 5.0-zvezdica iskustava sa tekstom.** Vlasnik sajta tražio da kartice sa maksimalnom ocenom i pravim tekstom budu vidljive prve (widget je defaultno mešao ocene 4.5–5.0 i placeholder kartice bez teksta — "Ovo iskustvo nema tekst, samo ocenu" — nasumično kroz karusel). Vendor ne nudi sort/filter parametar (samo `data-entity_slug`/`data-type`/`data-color`, vidi PDF), pa je rešenje client-side DOM reorder: nova komponenta `components/shared/HipokratijaWidget/HipokratijaWidget.tsx` (`"use client"`) rendersuje `.hipokratija-widget` div i u `useEffect`-u prati `MutationObserver` dok se ne pojavi vendor-ova poslednja kartica (`.last_slide`, "Pročitaj sva iskustva" CTA — signal da je fetch iskustava završen), zatim premešta postojeće `<li class="glide__slide">` DOM elemente (`appendChild`, ne clone — čuva glide.js interno stanje/event listenere) tako da kartice sa `score === 5` I stvarnim tekstom (ne vendor-ov literalni placeholder string) idu prve, ostale posle, CTA ostaje poslednja. `app/[locale]/page.tsx` sada koristi `<HipokratijaWidget entitySlug="..." />` umesto sirovog `<div>`. Verifikovano uživo (Playwright): od 11 kartica, 5 kvalifikujućih (sve 5.0 + pravi tekst) sad na pozicijama 0–4, karusel ‹ › navigacija i dalje ispravno radi posle reorder-a (testirano sa 3 uzastopna klika), 0 console/page grešaka. `npx tsc --noEmit` i `npm run lint` čisto (28, baseline nepromenjen). **Watch-item:** logika prepoznaje "nema teksta" kartice isključivo poređenjem sa tačnim vendor-ovim placeholder stringom ("Ovo iskustvo nema tekst, samo ocenu") — ako Hipokratija promeni taj tekst, reorder tiho prestaje da radi (nije rušeći, samo se vraća na njihov default redosled).
- 2026-08-05 — **Homepage statsSection — realne brojke umesto demo-seed placeholder-a.** Vlasnik sajta dostavio prave brojke: 19.000 procedura godišnje (ranije 15.000 "operacija"), 200 lekara specijalista (nepromenjeno), 50 godina iskustva (ranije 65), 146.000 poseta godišnje (ranije 50.000 "zadovoljnih pacijenata" — label promenjen jer je merena veličina druga). Nova skripta `scripts/migrate-homepage-stats-update.ts` (`npm run migrate:homepage-stats-update`) patch-uje `statsSection.stats` na produkcijskom `homepage` dokumentu (nalazi blok po `_type`, ne po hardkodovanom `_key`, isti obrazac kao `migrate-homepage-team-update.ts`); `label` polje je `localeString` (i18n schema) pa je upisano kao `{ _type: "localeString", sr, en: "" }`. Pokrenuta uživo, potvrđeno izlazom skripte. Seed u `migrate-all.ts` usklađen radi budućih punih re-seed-ova. `npm run lint` čist na dirnutim fajlovima (baseline greške u drugim, nepovezanim fajlovima nepromenjene).
- 2026-08-05 — **Header — uklonjene ikonice iz kontakt trake, header konačno `position: absolute` (skroluje se sa stranicom).** Vlasnik sajta poslao anotirani screenshot kontakt trake (telefon/@/pin ikonice pored broja/emailova/adrese) sa zahtevom da se ikonice uklone — `components/shared/Header/Header.tsx` `contactStrip` sad prikazuje samo tekst, `infoIcon` div-ovi uklonjeni (CSS klase ostavljene neiskorišćene radi lakog vraćanja, nesporan deo taska od početka).

  Ponašanje menija pri skrolu je prošlo kroz tri pogrešna tumačenja pre nego što je razjašnjeno: (1) "ne moj da navigacija bude sticky nego neka se scroluje gore" → protumačeno kao "nestani sa skrolom" (`fixed`→`static` + `PageHeader` padding 200px→80px); (2) vlasnik sajta reagovao da to nije dobro, tražio "treba da ostane isto onako kako je bilo samo da prati scroll" + "MENU ne treba da bude sticky, već da ostane na vrhu kad se krene scroll na dole" → protumačeno kao "vrati na `fixed`" (sve vraćeno na original); (3) vlasnik sajta, i dalje nezadovoljan, eksplicitno potvrdio da testira **lokalni** sajt (ne produkciju — bitno, jer dotad ništa u ovom tasku uopšte nije bilo push-ovano/deploy-ovano) i dao nedvosmislenu instrukciju: "postavi navigaciju u apsolutnu poziciju i kada krene da scroluje user stranicu neka se normalno skroluje sa ostatkom sadržaja gore" → **konačno rešenje**: `position: absolute` na `.mainHeader` (desktop i mobile breakpoint, koji je ranije koristio `sticky`). Bez pozicioniranog roditelja u `SiteChrome`/layout stablu, `absolute` se ponaša vizuelno identično `fixed`-u pri učitavanju stranice (header i dalje "pluta" preko hero slike, isti izgled kao pre) ali je deo normalnog document flow koordinatnog sistema pa se FIZIČKI skroluje zajedno sa sadržajem (za razliku od `fixed` koji ostaje zalepljen za viewport) — potvrđeno Playwright screenshotom da header nestaje sa ekrana već na ~300px skrola. `PageHeader` padding vraćen na originalne vrednosti (200px/100px/80px) jer `absolute`, baš kao `fixed`, i dalje izlazi iz normalnog flow-a pa unutrašnje stranice i dalje trebaju tu rezervisanu visinu.

  Verifikacija: `npm run lint` (28 problema, baseline nepromenjen), dev server + Playwright screenshot (globalni `npm -g playwright` paket sa keš-ovanim Chromium-om, pošto `chromium-cli` nije dostupan u ovom okruženju) na 0/300/900px skrola potvrdio ispravno ponašanje. **Napomena:** sve izmene iz celog dana (uklj. "Наш тим" i "Најновије вести" taskove) su i dalje samo lokalne/necommit-ovane — ništa push-ovano/deploy-ovano, vlasnik sajta testira preko sopstvenog lokalnog `npm run dev`.

- 2026-08-05 — **Homepage "Наш тим" — demo lekari zamenjeni pravima, uklonjene ikonice društvenih mreža.** Vlasnik sajta poslao anotirani screenshot: crveni okvir oko facebook/linkedin/email ikonica na kartici lekara ("Skloniti ove ikonice za društvene mreže") i zahtev da se ubace pravi lekari sa pravim fotografijama ("Ubaciti lekare i njihove slike iz priloga mejla") — 4 fotografije sa "Дедиње" logom na mantilu sačuvane u `public/images/nasi_strucnjaci/` (imena/specijalnosti u nazivima fajlova). `components/shared/TeamCard/TeamCard.tsx` — hover overlay sa social ikonicama sad uslovljen `socialLinks.length > 0` (ranije uvek renderovan, samo prazan bez linkova) — utiče na sve `TeamCard` korisnike (homepage + `/nas-tim`), ali samo demo-seed je ikad postavljao `socialLinks` (sve `"#"` placeholder), pa je ovo čisto bag-fix bez uticaja na pravi sadržaj. Nova skripta `scripts/migrate-homepage-team-update.ts` (`npm run migrate:homepage-team-update`) upload-uje 4 slike i patch-uje `homepage.pageBuilder[_type=="teamSection"].team` (potpuna zamena, ne append) sa: Др Јелена Кљајевић (Радиолог), Клин. асс. др сци. мед. Драгана Унић Стојановић (Анестезиолог са реаниматологијом), Др Милан Добрић (Интервентни кардиолог), Проф. др сци. мед. Иван Стојановић (Специјалиста минимално инвазивне хирургије) — bez `socialLinks` polja. Opisi namerno kratki/činjenični (specijalnost + naziv Instituta), bez izmišljenih tvrdnji (godine iskustva i sl. su bile u demo podacima, svesno NISU prenete na prave lekare bez potvrde vlasnika sajta). Verifikacija: `npm run lint` (28 problema, identičan baseline), dev server + server-rendered HTML potvrdio 4 nova imena prisutna/4 stara odsutna, `teamOverlay`/`teamSocial` CSS klase odsutne sa cele stranice (Footer-ove nepovezane social ikonice i dalje prisutne), Sanity CDN slike ispravno serving-uju kroz `_next/image` proxy. **Watch-item:** role/description tekst je minimalan placeholder, vlasnik sajta može dostaviti duži opis kroz Studio kasnije.

- 2026-08-05 — **Homepage: "Најновије вести" + "Гостовања у медијима" sekcija dinamički isključena, vesti premeštene u "Предстојећи догађаји" vidžet.** Vlasnik sajta poslao anotirane screenshotove: (1) donja sekcija sa dve kolone ("Најновије вести"/"Гостовања у медијима", dno homepage-a) precrtana crvenim X uz komentar "S obzirom da smo vesti ubacili gore, ovaj deo bih sklonio"; (2) "Добродошли" sekcija anotirana da se ispod postojećeg "Предстојећи догађаји" vidžeta doda isti-format blok, samo za poslednje 3 vesti, "isti format i fazon". Eksplicitno: dinamički ugasiti (ne obrisati) da se lako vrati. Rešenje: `HOMEPAGE_SECTION_TOGGLES` u `app/[locale]/page.tsx` proširen sa `newsGostovanja: false` flagom, cela "News + Gostovanja combined" `<Section>` uslovljena njime (isti obrazac kao ranija 4 toggle-a od ranije istog dana). Desna kolona `welcomeSection` bloka (dosad: `eventsWidget` ILI stari image/badge fallback) sada je `eventsColumn` wrapper (flex column, gap 24px) koji stack-uje DVA vidžeta istog vizuelnog stila (`.eventsWidget`, ista CSS klasa reciklirana): postojeći "Предстојећи догађаји" (kad ima ≥1 predstojeći event) i nov "Најновије вести" (kad ima ≥1 vest, prve 3 iz `homepageVesti`, ikonica `fa-newspaper`, linkuje na `/aktuelnosti/{slug}`, isti `eventItem`/`eventThumb`/`eventInfo`/`eventDate` markup kao events vidžet). Fallback (statička slika + bedž) i dalje prikazan samo kad NEMA ni događaja ni vesti. Nova CSS klasa `.eventsColumn` u `page.module.css`, ništa postojeće nije uklonjeno/preimenovano. Verifikacija: `npm run lint` — 28 problema, potvrđeno `git stash` poređenjem da je to identičan baseline (nulta nova greška/warning od ove izmene).

- 2026-08-05 — **WordPress migracija /aktuelnosti/vesti — 302 od 305 članaka, pagination, sortiranje, i18n support.** Vlasnik sajta dostavio WordPress export (`WordPress.2026-07-22.xml`, ikvbd.org stari sajt) sa 305 objavljenih blog-postova, svi pod jednom WP kategorijom ("aktuelnosti"). Nova migraciona skripta `scripts/migrate-vesti-wordpress.ts` (`npm run migrate:vesti-wordpress`): parsira WXR XML preko `fast-xml-parser` zavisnosti, uklanja WordPress Avada/Fusion Builder shortcode-ove i HTML, konvertuje plain paragraf tekst u `news.fullText` format, download-uje WordPress featured image iz zvaničnog `ikvbd.org` URL-a i upload-a u Sanity assets, auto-kategorizuje članke po srpskim ključnim rečima (kategorije: inovacije/akcije/oprema/uspeh/edukacija/obavestenje), generira ASCII Latin slug (WordPress percent-encoded UTF-8 slugove dekodira pa transliterira Ćir→Lat — bila je greška gde su raw percent-encoded stringovi korišćeni kao ID, ispravljeno). Rezultat: **302/305 importovano** (3 starija članka iz 2020/2021 bez dostižne featured slike su preskočena i logirana skriptom), 6 starih placeholder `news` dokumenata obrisano. `/aktuelnosti/vesti` stranicu iz unpaginiranog grida sa svim člancima (fine za 6 fake stavki, neupotrebljivo za 302) refaktorisana u: thin server component `app/[locale]/aktuelnosti/vesti/page.tsx` koji fetch-uje i delegira client komponenti `VestiClient.tsx`, nova komponentu modelovana na `gostovanja/GostovanjaClient.tsx` pattern (search, filter po kategoriji, sort po datumu, "Prikaži još" load-more). Hero sekcija (1 featured kartica + 2 sekundarne) zadržana, unpaginirana, kao i pre. Nova helper funkcija `app/[locale]/aktuelnosti/categoryLabels.ts` mapira `news.category` enum vrednosti na Cyrillic display labele, korišćena i u listi i u detail stranici (`[slug]/page.tsx`); detail stranica takođe dobila uslovljenu kategoriju badge sa labeli mapiranjem umesto raw enum vrednosti. **npm run lint:** svi dirani fajlovi prolaze, dev server Playwright smoke-test + real WordPress fotografije/Cyrillic tekst/search+filter+load-more svi 0 console grešaka. **Watch-item:** ostale Aktuelnosti podsekcije (`gostovanja`, `obavestenja`, `oglasi-konkursi`, `casopis-dedinje`, `informator`) ostaju placeholder (nisu pokrivene WordPress export-om, eksplicitno su OUT scope-a ovog taska).

- 2026-08-05 — **Footer redizajn — tri lokacije umesto starih linkova/kontakta sekcija.** Vlasnik sajta zahtevao nove raspored footer-a: колona 1 (logo/opis/društvene mreže) ostaje ista, a kolone 2-4 sada prikazuju tri lokacije Instituta — "ДЕДИЊЕ 1", "ДЕДИЊЕ 2", "ДЕДИЊЕ 3" — svaka sa naslovom, ugrađenom Google Maps mapom (iframe, isti obrazac kao na `/kontakt` i `/o-nama/lokacija`) i adresom ispod mape. Kolone "Brzi linkovi", "Usluge" i stari "Kontakt" blok (telefon/email/radno vreme) su uklonjene iz footer-a. Sanity šema `sanity/schemas/singletons/footer.ts` — uklonjena polja `quickLinks`, `services`, `contact`; dodato novo polje `locations` (array, max 3, tip `footerLocation`: title/localeString, mapEmbedUrl/url, address/string, city/string). `sanity/lib/queries.ts` `FOOTER_QUERY` ažuriran; `sanity/types.ts` `Footer` interface ažuriran; `components/shared/Footer/Footer.tsx` renderuje 3 kolone lokacija; `Footer.module.css` `.footerGrid` grid-template-columns promenjen na `1.2fr 1fr 1fr 1fr`. Adrese za sve tri lokacije su privremeno iste (Хероја Милана Тепића бр. 1, 11040 Београд) dok vlasnik sajta ne dostavi tačne adrese/koordinate za Дедиње 2 i 3. NIJE urađeno: Sanity dataset nije seed-ovan (migracija skripta `scripts/migrate-footer.ts` ažurirana ali nije pokrenuta — vlasnik sajta treba da pokrene `npm run migrate:footer` ili ručno popuni `locations` u Studio-u). Verifikacija: `npm run lint` čist na dirnutim fajlovima (samo pre-postojeći `any`-tip errori u `sanity/types.ts` nepovezani sa ovim taskom, i pre-postojeći `<img>` warning u Footer.tsx), dev server potvrdio 4 kolone i adrese (mape vizuelno nisu vidljive u headless test okruženju, isti problem kao na /kontakt, ne regresija).

- 2026-08-05 — **Nova homepage sekcija "Наши партнери" (logo-grid, demo).** Vlasnik sajta tražio novu sekciju ispod "Клинике и службе" sa logotipima partnerskih klinika/bolnica/institucija u Evropi i svetu, sa konkretnom demo listom od 9 institucija (Cleveland Clinic, Бакуљов центар, Fuwai Hospital, Светска банка, Медицински факултети Београд/Крагујевац, Алмазов центар, УКЦ Ниш, Ватерполо репрезентација Србије). Postojeći `partnersSection` Sanity pageBuilder blok (samo FontAwesome ikonica + ime, bez pravih logo slika, renderovan na dnu stranice) nije odgovarao potrebi — nova sekcija je hardkodovan `PARTNER_INSTITUTIONS` niz u `app/[locale]/page.tsx`, nezavisan od Sanity-ja. Pitano vlasnika sajta odakle logo slike: potvrdio "probaj sa njihovog sajta, ako bude loše videćemo". Logotipi preuzeti `curl`-om direktno sa zvaničnih sajtova institucija (World Bank izuzetak — sajt je JS-rendered SPA bez logo fajla u raw HTML-u, uzet isti zvaničan logo sa Wikimedia Commons) i sačuvani u novi `public/images/partners/`. **Fuwai Hospital nema logo** — zvaničan kineski sajt nedostupan sa naše mreže (timeout na sve varijante), nema ga ni na Wikipediji — kartica pada na tekstualni fallback dok vlasnik sajta ne obezbedi fajl. Nove CSS klase (`.partnersLogosGrid` i prateće) u `page.module.css`, responsive 5→3→2 kolone. Svaka kartica linkuje na zvaničan sajt institucije (`target="_blank"`), grayscale→color hover efekat. **Watch-item:** logotipi nisu formalno licencirani za komercijalnu upotrebu na javnom sajtu — vlasnik sajta svestan i treba da ih pregleda/zameni pravim materijalom pre pravog lansiranja (isto upozorenje kao postojeći demo-seed watch-item za teamSection/testimonialsSection ispod). Verifikacija: `npm run lint` (27 problema, baseline nepromenjen), dev server + Playwright screenshot potvrdio sekciju (svih 9 kartica, hover efekat, Fuwai fallback bez duplog teksta, 0 grešaka u konzoli).

- 2026-08-05 — **Homepage sekcija "Наше клинике" proširena sa 6 na svih 13 klinika/službi (po specifikaciji vlasnika sajta).** Vlasnik sajta poslao mokap postojeće sekcije (6 kartica + dugme "Погледајте све клинике") sa anotacijom da treba izlistati punu listu od 13 klinika/službi umesto skraćene. Svih 13 stavki iz specifikacije mapirano 1:1 na već postojeće `clinicPage` Sanity dokumente/rute (nijedna nova klinika/služba nije trebalo da se kreira — sve već postoje kao pojedinačne stranice pod `/klinike/<slug>`, uočeno u postojećem `FALLBACK_CLINICS` nizu na `/klinike` hub stranici): dodato 7 novih kartica (KV KT/KV MR dijagnostika → `kv-dijagnostika`, Telemedicina → `telemedicina`, Poliklinika → `poliklinika`, Kardiovaskularna rehabilitacija → `kardiovaskularna-rehabilitacija`, Apoteka → `apteka`, Laboratorijska dijagnostika → `laboratorija`, Banka krvi → `transfuzija`) uz postojećih 6. Promene: (1) `CLINICS_FEATURED` fallback niz u `app/[locale]/page.tsx` proširen na 13 stavki; (2) nova jednokratna migraciona skripta `scripts/migrate-homepage-clinics-update.ts` (`npm run migrate:homepage-clinics-update`) patch-uje `pageBuilder[_type=="clinicsFeaturedSection"].items` na `homepage` Sanity dokumentu sa istih 13 stavki — pokrenuta na produkciji, potvrđeno upitom (dev server HTML sadrži svih 13 naslova, sve 7 novih `/klinike/<slug>` ruta vraćaju HTTP 200). Sadržaj (naslov/opis/ikonica) zadržava kratki stil postojećih 6 kartica (bez "Клиника за"/"Служба за" prefiksa), radi vizuelne doslednosti grida. `npm run lint` (27 problema, baseline nepromenjen, nijedna nova greška u dirnutim fajlovima).

- 2026-08-05 — **4 homepage sekcije privremeno isključene (dinamički toggle, ne brisanje).** Vlasnik sajta poslao anotirane screenshotove homepage-a sa crvenim X preko 4 bloka koje trenutno želi da ugasi: "Комплетна кардиоваскуларна нега" (`servicesSection`), "По чему смо другачији" (`whyChooseUsSection`), citat direktora + "Ваше здравље је наш приоритет" CTA baner (dva odvojena JSX bloka — hardkodovana "Director's Word" sekcija i `ctaSection`), i "Контактирајте нас" + Google mapa (hardkodovana `contactCta` sekcija). Eksplicitan zahtev: ugasiti **dinamički**, ne trajno obrisati, da se lako vrate ako vlasnik sajta promeni mišljenje. Rešenje: nov `HOMEPAGE_SECTION_TOGGLES` objekat (5 boolean flagova, svi `false`) na vrhu `app/[locale]/page.tsx`, svaki od 5 pogođenih JSX blokova uslovljen odgovarajućim flagom (`{HOMEPAGE_SECTION_TOGGLES.services && servicesSection && (...)}` itd.) — vraćanje sekcije je promena jedne `false` → `true` vrednosti, bez diranja JSX/CSS/Sanity podataka. Ništa nije obrisano iz koda, `.module.css` klasa ni Sanity šeme/sadržaja. Verifikacija: `npx tsc --noEmit` čisto, `npm run lint` (27 problema, baseline nepromenjen), dev server + HTML provera potvrdila da svih 5 blokova (4 vizuelne grupe) nestaju sa stranice dok ostatak (uklj. nov "Предстојећи догађаји" vidžet iz prethodnog taska istog dana) ostaje netaknut.

- 2026-08-05 — **Novi `event` Sanity dokument tip + homepage "Предстојећи догађаји" vidžet.** Site vlasnik poslao mokap (screenshot) postojeće homepage "Добродошли" sekcije sa anotacijama: levi tekst treba da uklopi puni dodatni institucionalni tekst, desna slika (foto-kolaž + "65+ ГОДИНА ИСКУСТВА" bedž) treba da postane vidžet predstojećih događaja. Pošto strukturisan "događaji" sadržaj (naslov + realan sortabilan datum + slika) nije postojao nigde na sajtu (najbliže: `announcement` tip bez slike, i tekstualne kongres/radionica kartice u Edukaciji bez strukturisanog datuma), kreiran je nov sadržajni tip. Detalji: nova Sanity šema `sanity/schemas/documents/event.ts` (`title` localeString, `date` Sanity date tip, `image` obavezno, `location` plain string, `link` opciono, **`enReviewed` boolean uključen od početka**, isti obrazac kao ostali Aktuelnosti tipovi), registrovana u `schemas/index.ts` i `structure.ts` (pod Актуelности, između Вести i Гостовања) — minimalna verzija, bez posebne detalj-rute. Nov `EVENTS_QUERY` u `sanity/lib/queries.ts`, `EventItem` tip u `sanity/types.ts`. Homepage (`app/[locale]/page.tsx`) sada fetch-uje događaje paralelno sa vestima/gostovanjima, filtrira `date >= danas` i uzima prva 3; desna kolona `welcomeSection` bloka prikazuje nov vidžet (mala slika + naziv + datum + lokacija po događaju) kad ima bar 1 predstojeći događaj, inače pada nazad na stari layout (slika/bedž — ta polja u šemi nisu uklonjena, ostaju kao fallback izvor, isti princip kao "disable ne delete" primenjen na video slajder 2026-08-04). Nova CSS klasa `.eventsWidget` i prateće (`app/[locale]/page.module.css`); `.welcomeGrid` promenjen sa `align-items: center` na `align-items: start` (duži tekst bi inače centrirao kraći vidžet neprirodno na sredini kolone — verifikovano Playwright screenshotom pre/posle promene). Nova `.welcomeBodyText { white-space: pre-line; }` klasa da `\n\n`-razdvojeni pasusi u Sanity `bodyText` polju prave vizuelne prelome. Sadržajna izmena na produkciji (Sanity patch, NE code change — jednokratni scratchpad skript kreiran, pokrenut, pa obrisan, isti obrazac kao hero-video patch 2026-08-04): `homepage.welcomeSection.bodyText.sr` proširen sa postojeće jedne rečenice na tu rečenicu + 5 novih pasusa teksta koji je vlasnik sajta dostavio ("Више од 45 година...", zaključno sa "Придружите нам се.../Ваш Институт..." pasusima); `bodyText.en` očišćen na `""` (pada nazad na SR na `/en` preko postojećeg `localize()` fallback-a, čeka standardnu i18n export/translate/apply pipeline). `leadText` nije menjan (već se poklapao sa mokapom). Migraciona skripta `scripts/migrate-events.ts` (`npm run migrate:events`) seed-uje 3 primer događaja — imena preuzeta iz već poznatih ponavljajućih Institut-skih događaja pomenutih drugde na sajtu (Годишњи конгрес Института, Сретењска радионица, Конгрес кардиоваскуларне превенције/ХИСПА), slike reciklirane iz postojećih `public/images/*` fajlova. **Watch-item / Backlog:** datumi (2027-02-13, 2027-04-15, 2027-06-10) su procena/placeholder na osnovu istorijskog obrasca pominjanog drugde na sajtu, NISU potvrđeni od vlasnika sajta — treba potvrda pravih datuma kroz Studio (isti obrazac kao postojeći "Часопис Дедиње"/"Информатор" flagovi u Blokerima ispod). Verifikacija: `npx tsc --noEmit` čisto, `npm run lint` (27 problema, baseline nepromenjen), `npm run build` (exit 0), dev server + Playwright screenshot potvrdili vidžet sa 3 događaja (slike/datumi/lokacije), fallback ispravno odsutan, puni tekst renderovan sa ispravnim prelomima pasusa.

- 2026-08-05 — **Glavni meni: reachability za `/za-pacijente` i `/nas-tim` + active-page highlighting.** Nastavak prethodnog taska (info kartice). Problem: dve nove rute (`/za-pacijente` hub, `/nas-tim`) nisu bile dostižne iz navigacionog menija (bez linkova ka njima), i meni nikad nije označavao aktivnu stranu. Rešenja: (1) **Nav reachability:** `scripts/migrate-navigation.ts` ažuriran da doda novu top-level stavku "НАШ ТИМ" (`_key: "nas-tim"`) između "О НАМА" i "КЛИНИКЕ" sa linkom `/nas-tim`; postojeća stavka "ЗА ПАЦИЈЕНТЕ" (koja je do sad imala samo submenu, bez vlastite rute) dobila `link: "/za-pacijente"` da bude i hub link. `components/shared/Header/Header.tsx` render-logika ispravljena: top-level stavke sa OBEMA `link` i `submenu` sada se renderuju kao `<Link>` (klikljiv tekst vodi na hub), sa odvojenim chevron ikonom koji `preventDefault+stopPropagation` otvara dropdown bez navigacije — starim stavkama (О НАМА, NAUKA, EDUKACIJA, AKTUELNOSTI) bez sopstvenog linka ostaje samo toggle dugme. (2) **Active-page highlighting:** `Header.tsx` sada koristi `usePathname` iz `@/i18n/navigation` (ne plain `next/navigation`) za locale-aware putanje. `isLinkActive()` i `hasActiveDescendant()` helper funkcije (useCallback) označavaju aktivne linkove i njihove parent skupove sa `.active` CSS klasom. `Header.module.css` proširio je `.active` pravila sa novim selektorima za button/dropdown stavke i mobile media query-jima. **Watch-item:** lokalni dev caching — posle ažuriranja `navigation` dokumenta u Sanity, ceo `.next` direktorijum (ne samo `.next/cache`) mora biti obrisan pre nego što dev server preuzme svežu šemu; to je Turbopack persistent cache, ne production problem. `npm run lint` (27 problema, baseline nepromenjen), `npm run build` (exit 0), Playwright verificirani: nav sada prikazuje НАШ ТИМ i ЗА ПАЦИЈЕНТЕ linkove, active-state highlighting radi ispravno kroz sve 3 nivoa menija.

- 2026-08-05 — **Homepage info kartice redizajnirane + nove landing stranice `/za-pacijenте` i `/nas-tim`.** Nova `contact` varijanta za `infoBox` object tip u Sanity (`sanity/schemas/objects/infoBox.ts`, dodata `contactPhone`/`contactFax` polja). Sadržaj 4 info kartice ispod hero slajdera ažuriran na Sanity `homepage` dokumentu — nove kartice: Информације за пацијенте (linkHref `/za-pacijente`), Наше клинике (linkHref `/klinike`), Наш тим (linkHref `/nas-tim`, ranije `#team` anchor), Контакт (novi contact variant sa telefon/faks brojevima — 011 3601 700 i 011 2666 445, zamenjuje stari `emergency` variant). Sekcija "За пацијенте" (6 brzih linkova) **premeštena** iz homepage JSX-a na novu landing stranicu: `app/[locale]/za-pacijente/page.tsx` + `metadata.ts`, koristi postojeću `patientLinksSection` pageBuilder blok iz Sanity `homepage` dokumenta preko novog `PATIENT_LINKS_SECTION_QUERY` u `sanity/lib/queries.ts`; JSX sekcija i fallback konstanta (`PATIENT_LINKS`) uklonjeni iz `app/[locale]/page.tsx`. Nova ruta `/nas-tim` (`app/[locale]/nas-tim/page.tsx` + `metadata.ts`) čita `teamSection` pageBuilder blok sa homepage-a preko novog `TEAM_SECTION_QUERY`, renderuje isti `TeamCard` kao homepage (originalna inline sekcija "Тим" na početnoj je zadržana i dalje). `app/sitemap.ts` ažuriran sa `/za-pacijente` i `/nas-tim`. Migraciona skripta `scripts/migrate-all.ts` i `scripts/migrate-homepage-cards-update.ts` (nova skripta, já pokrenuta na produkciji) ažurirani; sadržaj potvrđen upitom na Sanity API. `npm run lint` (27 problema, baseline nepromenjen), `npm run build` (exit 0), dev server smoke-test (`/`, `/za-pacijente`, `/nas-tim` HTTP 200 sa očekivanim sadržajem). **Napomena:** `/za-pacijente` landing je centralna stranica sa linkovima na 12 pojedinačnih `/za-pacijente/*` ruta — NIJE isto što je backlog item "О вашем здрављу" (koji bi bio kategorijalna group-stranica za članke), to su odvojene stavke (vidi TASKS.md backlog).

- 2026-08-04 — **Homepage hero: 4-slajd video slajder → jedan statičan video.** Vlasnik sajta odlučio da ukine slajder ideju za hero — samo jedan video (poslat fajl, sačuvan u `public/videos/video-za-slajder.mp4`) sa H1/H2 tekstom preko njega ("Национални институт за срце и крвне судове" / "Традиција и поверење које траје 50 година."). Eksplicitan zahtev: **slajder samo disablovati, ne uklanjati iz koda** — i sve mora ići kroz Sanity. `HeroSection.tsx` već je imao ugrađenu podršku za to: `isSlider = slides.length > 1`, pa strelice/tačkice/auto-play logika ostaju netaknute u kodu, samo se ne renderuju kad ima samo 1 slajd. Promene: (1) Sanity `homepage` dokument, `heroSlidesSection.slides` patch-ovan sa 4 slajda na 1 (jednokratni scratchpad skript, ne dodat u `scripts/` jer je sadržajni patch a ne ponovljiva migracija) — SR/EN tekst upisan u `localeString`/`localeText` obliku, bez badge-a; (2) `videoHeroSlides` fallback niz u `app/[locale]/page.tsx` (koristi se samo ako Sanity fetch ne uspe) usklađen na isti jednoslajdni sadržaj. Terminologija za EN prevod ("Institute for Cardiovascular Diseases Dedinje", "heart and blood vessels") preuzeta iz postojećeg `siteSettings.siteDescription` radi doslednosti. Verifikovano: `npm run lint` (27 problema, baseline nepromenjen), `npm run build` (exit 0), i ručna provera u browseru (dev server + Playwright screenshot) na `/sr` i `/en` — video se autoplay-uje (frejm se menja između dva screenshot-a), H1/H2 ispravno prikazani, nema strelica/tačkica slajdera. `HeroSlider.tsx` (stariji, odvojen, orphaned komponent — ne koristi se nigde u `app/`) nije diran, nezavisan od ovog taska.

- 2026-08-04 — **Interni linkovi gubili `/en` prefiks pri navigaciji — bag-fix.** Vlasnik sajta prijavio: kad se izabere engleski i klikne bilo koji link u navigaciji, URL se vraća na srpsku verziju (gubi `/en`). Uzrok: skoro svi interni `<Link>` pozivi kroz sajt (nav meni u `Header.tsx`, `Footer.tsx`, i deljene kartične komponente — `Button.tsx`, `ClinicCard.tsx`, `CardGrid.tsx`, `GradientCard.tsx`, `ProgramCard.tsx`, `PageHeader.tsx` (breadcrumb), `components/typography/Link.tsx`, plus direktni pozivi u `app/[locale]/page.tsx` i aktuelnosti/vesti/gostovanja stranicama) su importovali obično `next/link`, koje ne zna ništa o trenutnom locale-u — href kao `/klinike` uvek vodi na srpsku (bez-prefiksnu) verziju, bez obzira na to da li si trenutno na `/en/*`. next-intl već ima drop-in zamenu (`Link` iz `@/i18n/navigation`, exportovan iz `createNavigation()`) koja automatski čuva trenutni locale u href-u. Ispravka: **15 fajlova** prebačeno na `import { Link } from "@/i18n/navigation"` (`components/typography/Link.tsx` alias-ovan kao `NextLink` jer taj fajl već izvozi sopstvenu komponentu po imenu `Link`). `LanguageSwitch.tsx` NAMERNO izuzet — on ručno gradi cross-locale hreference (to mu je posao, jedini slučaj gde link SME da promeni locale). Provereno da next-intl-ov `Link` ne dira hrefove koji ne počinju sa `/` (`tel:...`, `mailto:...`, apsolutni Sanity CDN URL-ovi za PDF download) — ostaju netaknuti, testirano uživo (`tel:0113601700` dugme na `/en/za-pacijente/ambulante` ostaje `tel:0113601700`, ne postaje `/en/tel:...`). `npx tsc --noEmit` i `npm run lint` bez novih grešaka. Verifikovano Playwright-om: nav meni na `/en` (svih ~50 stavki) sad ima `/en/*` hrefove, klik na "CLINICS" ide na `/en/klinike`, footer linkovi ka internim rutama takođe nose `/en` prefiks, višestruki uzastopni klikovi (kontakt → logo/početna) ostaju na `/en` kroz ceo lanac.

- 2026-08-04 — **StatCounter (homepage "Бројке које говоре") — bag-fix, otkriven uzgred pri proveri LanguageSwitch-a.** Vlasnik sajta prijavio da brojke na srpskoj početnoj ostaju "0" dok na `/en` prikazuju tačne vrednosti. Uzrok: **isti bag kao treći bag iznad, samo na drugoj komponenti** — `StatCounter.tsx` broji uživo (`setState` svaki animation frame), ali globalni transliteration engine (`transliterateDom.ts`) keš-uje "originalni" tekst text-node-a SAMO pri prvom viđenju (WeakMap), pa svaki sledeći React re-render tog node-a MutationObserver odmah prepisuje nazad na keširanu (početnu, "0") vrednost — beskonačan tug-of-war koji vizuelno zamrzava brojač na 0. Na `/en` se ovo ne vidi jer `ScriptProvider` tamo uopšte ne pokreće transliteraciju (zaštita dodata u prethodnom fix-u ovog dana) — što je slučajno "sakrilo" ovaj pre-postojeći bug samo za engleski. Bug je postojao i pre i18n rada, nezavisno od njega. Ispravka: `data-no-translit` atribut na `.statNumber` div u `StatCounter.tsx` (brojke ionako ne treba nikad da se transliterišu — cifre su iste u oba pisma). Verifikovano uživo (dev server + Playwright): srpska početna sad ispravno animira do 15.000/200/65/50.000, i dalje radi ispravno i u ćirilica i u latinica modu (labele se transliterišu, brojke ostaju netaknute).

- 2026-08-04 — **i18n Faza 3e (nastavak) + kritični bag-fixevi + UI prebidač za jezik.** Urađen kao dopuna/završetak Faze 3e i Faze 3f konteksta:
  - **Dodat UI prekidač za jezik/pismo** (`components/shared/LanguageSwitch/LanguageSwitch.tsx`) — zamenjuje stare `components/shared/LocaleSwitch/` i `components/shared/ScriptToggle/` (oba obrisana). Nudi kompaktnu kontrolu sa 3 opcije (English, Српски ћирилица, Srpski latinica) — dropdown u desktop headeru, pill-varijanta u mobile meniju. Otkriven i ispravljen kritični arhitekturni bag: Header/Footer se rendivali iz `app/layout.tsx` (root layout, van `[locale]` segmenta) — nisu se ažurivali nakon soft navigacije između `/` i `/en` jer Next.js App Router ne re-renderuje server-component stablo iznad gde se URL segmenti granaju. Ispravka: `SiteChrome`/`HeaderData`/`Footer` premešteni iz `app/layout.tsx` u `app/[locale]/layout.tsx` (unutar `NextIntlClientProvider`-a), sad se ispravno re-renderuju. `app/layout.tsx` sada samo renderuje `{children}` bez header/footer. `SiteChrome.tsx` pojednostavljen (uklonjena `pathname.startsWith("/studio")` provera jer `/studio` vivi van `[locale]` segmenta i nikad se ne renderuje kroz njega).
  - **Otkriven i popravljen drugi bag:** next-intl middleware je podrazumevano imao `localeDetection: true` (automatska detekcija preko `NEXT_LOCALE` kolačića). Posledica: kad korisnik poseti `/en`, middleware bi postavio kolačić; svaki sledeći zahtev ka `/` bi bio 307-redirektovan nazad na `/en` zbog kolačića, čak i posle klika na "Srpski" u novom prekidaču. Ispravka: `i18n/routing.ts` sada eksplicitno postavlja `localeDetection: false` (URL je jedini izvor istine za locale).
  - **Otkriven i popravljen treći bag:** globalni transliteration engine (`transliterateDom.ts`) je greškom primenjivao ćirilicu→latinicu konverziju i na tekstove samog `LanguageSwitch` prekidača, što je moglo da pokvari prikaz opcija. Dodat `data-no-translit` atribut na wrapper div-ove u `LanguageSwitch.tsx` (postojeći mehanizam izuzimanja u `transliterateDom.ts` već podržan).
  - **Dodata zaštita:** `ScriptProvider.tsx` sada preskače transliteraciju kad je putanja `/en/*` (engleski sadržaj se nikad ne transliterira).
  - **Verifikacija:** `npm run lint` prolazi bez novih grešaka (baseline nepromenjen), testirano uživo kroz dev server + Playwright (screenshot + klik) — pun krug EN → SR ćirилица → SR latinica → EN radi bez grešaka u konzoli, na desktop i mobile viewport-u.
  - **Faza 3f status:** čeka ljudski review — vlasnik sajta pregleda AI prevode u Studio-u i čekira `enReviewed` po dokumentu; tek onda se sitemap gate uklanja i i18n task zatvara.

- 2026-08-04 — **i18n Faza 3d (AI prevod sadržaja) — ✅ gotova.** Nastavak posle Faze 3c/3e (prošla sesija).
  - **Promena u odnosu na originalni plan:** umesto headless `scripts/translate-content.ts` sa `@anthropic-ai/sdk`/`ANTHROPIC_API_KEY`, prevod odrađen direktno u Claude Code sesiji — vlasnik sajta potvrdio ovaj pristup kad je postavljeno pitanje čemu služi poseban API ključ. Bez novog paketa, bez novog ključa, bez API troška po pozivu.
  - `scripts/i18n-export-untranslated.ts` (schema-driven, isti pristup kao `migrate-i18n-schema.ts`) izvukao **2742 pojedinačna polja** (ne 423 — to je bio broj top-level dokument-polja iz 3c; 2742 su leaf `{sr,en}` stringovi unutar njih), 99 dokumenata, 0 `localePortableText` zadataka (jedina 4 mesta gde se taj tip koristi — `doctor`/`service`/`department` tipovi — nemaju nijedan dokument u datasetu, orphaned).
  - 2742 zadatka podeljeno u 11 batch-eva (grupisano po dokumentu) i prevedeno **paralelno preko 11 Agent poziva** (formalni medicinski/institucionalni registar). Spojeno i validirano: 2742/2742 pokriveno, 0 duplikata/propusta/nepoznatih ključeva; 49 slučajeva `en === sr` ručno pregledano — svi legitimni (akronimi, imena, brojevi, engleski nazivi radionica).
  - `scripts/i18n-apply-translations.ts` upisao sve u `production` preko granularnog Sanity patch path-a (npr. `pageBuilder[_key=="xxx"].heading.en`) + `enReviewed: false` eksplicitno po dokumentu. Testirano `--dry-run` + smoke-test na 1 dokumentu pre punog upisa (isti oprez kao 3c). Rezultat: 99/99 dokumenata, 2742/2742 polja, 0 grešaka; ponovni export posle upisa → 0 preostalih (potvrđena potpunost/idempotentnost).
  - `enReviewed: boolean` (human review gate) dodato na svih 20 document/singleton schema tipova sa lokalizovanim sadržajem, **granularnost po dokumentu** (99 flagova) — vlasnik sajta eksplicitno odabrao ovu opciju umesto po-polju (423+ checkbox-ova bi bilo nepraktično u Studio-u).
  - `app/sitemap.ts` — dok klinika/za-pacijente dokument nema `enReviewed: true`, `/en/*` varijanta te stranice se izostavlja iz sitemap-a (fetch preko posebnog `useCdn:true` klijenta da ne pokvari `force-static`). Watch-item otkriven i ispravljen tokom verifikacije: 2 za-pacijente dokumenta (`kardiologija-za-pacijente`, `vaskularna-hirurgija-za-pacijente`) imaju Sanity slug različit od URL segmenta — sitemap ih je prvo pogrešno propustio kroz gate dok nije dodata eksplicitna override mapa. `noindex` meta tag NIJE dodat (samo sitemap) — pravi noindex zahteva da ~70 statičnih `metadata.ts` fajlova fetch-uje Sanity, postojeći veći Backlog item, van scope-a.
  - **Verifikacija:** `npx tsc --noEmit` čisto, `npm run lint` (27 problema, identično baseline-u), `npm run build` (exit 0). Ručna provera u browseru (dev server): 5 `/en/*` rute (uklj. slug-override slučaj), svi 200, 0 `[object Object]`, engleski tekst se stvarno renderuje. `sitemap.xml` provera potvrdila gating radi ispravno.
  - **Sledeći korak:** Faza 3f — čeka da vlasnik sajta pregleda prevode u Studio-u (tabbed sr/en input) i čekira `enReviewed` po dokumentu; tek onda se sitemap gate uklanja i i18n task zatvara. Ljudski korak, ne nastavlja se automatski.
- 2026-08-04 — **i18n Faze 3c (migraciona skripta) + 3e (ožičenje frontend-a) — obe ✅ gotove, urađene zaredom u istoj sesiji.** Nastavak posle Faze 3b (schema lokalizacija).
  - **Faza 3c:** `scripts/migrate-i18n-schema.ts` (`npm run migrate:i18n-schema`) — **schema-driven** pristup umesto ručno pisane allowlist mape iz originalnog plana: skripta čita stvarne schema definicije iz `sanity/schemas/index.ts` i rekurzivno transformiše svaki fetch-ovan dokument prateći tu strukturu (nema rizika da se allowlist i schema razminu). Testirano `--dry-run` protiv `production` (99 dokumenata/423 polja identifikovano ispravno), pa pokrenuto stvarno **direktno na `production`** — vlasnik sajta eksplicitno odobrio bez kloniranja test-dataset-a ("sajt je još u izradi, nema rizika, sve će se ionako menjati"). Rezultat: 99/99 dokumenata migrirano, potvrđena idempotentnost (drugi `--dry-run` → 0 promena).
  - **Posledica (očekivana, ne bug):** odmah posle 3c upisa, **ceo sajt je vratio 500 grešku** na svakoj Sanity-backed stranici — frontend (`PageBuilder.tsx`, `ClinicPageTemplate.tsx`, svih ~78 `page.tsx`) je i dalje čitao `title`/`subtitle`/itd. kao plain string, a sad su to `{sr,en}` objekti (React baca na pokušaju renderovanja objekta kao child). Potvrđeno curl-om na `/klinike/kardiologija` pre fix-a.
  - **Faza 3e (fix + originalno planiran task, urađen zajedno):** umesto `pick(field, locale)` poziva razbacanih po ~150 fajlova (PageBuilder + ~30 leaf komponenti + svaki page.tsx), dodata jedna funkcija `localize<T>(data, locale)` u `sanity/lib/locale.ts` — rekurzivno vraća fetch-ovan podatak u **identičan plain-string oblik** kakav je bio pre 3b/3c (prepoznaje `_type` markere, isti EN→SR fallback kao `pick()`). Poziva se samo na fetch call-site-u (odmah posle `client.fetch`), pa PageBuilder/template/leaf komponente ostaju **potpuno netaknute**. Rezultat: svih **83 fetch call-sites** (81 u `app/`, 2 u `components/`) sad prosleđuju kroz `localize()` — 57 uniformnih fajlova (klinike/za-pacijente/o-nama/edukacija/schoolPage/nauka-istrazivanje) odrađeno kroz 2 Node skripte (string-replace, pattern bio bajt-identičan osim SLUG/naziv funkcije), ostatak (homepage, klinike hub, kardiohirurgija+units, kontakt, aktuelnosti hub+6 podstranica+2 metadata.ts, rec-direktora/o-institutu/biografija/bibliografija+4 metadata.ts, `HeaderData.tsx`/`Footer.tsx` sa `getLocale()` iz `next-intl/server`) ručno.
  - **Usput popravljen orphan bug** (nevezan za i18n): `app/[locale]/bibliografija/metadata.ts` postojao ali `page.tsx` ga nikad nije re-eksportovao — stranica nikad nije imala SEO metadata. Dodato.
  - **Watch-item (svesno odloženo):** ~70 `metadata.ts` fajlova (client-page metadata obrazac) namerno NIJE dirano — svi vraćaju statičan SR-only tekst i nikad nisu ni čitali Sanity `seo` polje (ni pre i18n taska), pa `{sr,en}` migracija ih ne dotiče (nisu se pokvarili). I dalje SEO-suboptimalno na `/en/*` (srpski `<title>`), zaveden u `TASKS.md` Backlogu kao poseban zadatak.
  - **Verifikacija:** `npx tsc --noEmit` čisto, `npm run lint` (27 problema, identično baseline-u), `npm run build` (exit 0). **Ručna provera u browseru** (ne samo build) — pokrenut dev server, ~25 ruta testirano na `/` i `/en/*` (svi glavni tipovi sadržaja + kardiohirurgija unit ruta + `/studio`), svi HTTP 200, bez `[object Object]` u HTML-u, EN rute ispravno padaju nazad na SR tekst (očekivano dok Faza 3d ne popuni prevode).
  - **Sledeći korak:** Faza 3d (AI prevod, `scripts/translate-content.ts`) — svih 423 migrirana polja trenutno ima `en: ""`.
- 2026-08-04 — **i18n Faza 3b Korak 2 (pun rollout, svih 51 schema fajlova) — ✅ Faza 3b u potpunosti gotova.** Nastavak posle vlasnikove potvrde tabbed sr/en Studio UX-a (Korak 1). Isti mehanički obrazac primenjen kroz 5 batch-eva (12 dokument tipova → 8 singletona → 16 + 15 object/block tipova), sa `npx tsc --noEmit`/`npm run build`/`npm run lint` proverom posle svakog batch-a (27 problema, baseline nepromenjen sve vreme, nula regresija):
  - **Dokument tipovi (12):** `news.ts`, `clinicPage.ts` (najveći, ~15 polja uklj. ugnježdene nizove stats/highlights/areas/staffList/units), `announcement.ts`, `jobPosting.ts`, `magazineIssue.ts`, `schoolPage.ts`, `service.ts`, `department.ts`, `doctor.ts`, `testimonial.ts`, `video.ts` — svi `slug` `source` i `preview.select` prebačeni na `.sr` varijante. **`publication.ts` svesno preskočen** — bibliografski citati (naslov rada/abstrakt/časopis) se ne prevode kao marketinški tekst, i tip nigde nije ožičen na frontend (potvrđeno grep-om).
  - **Singletoni (8):** `navigation.ts`, `siteSettings.ts`, `footer.ts`, `aboutPage.ts`, `bibliographyPage.ts` (`categories[].publications[].text` isto svesno neizmenjen, isti razlog kao `publication.ts`), `biographyPage.ts`, `directorPage.ts`, `informatorPage.ts`.
  - **Object/block tipovi (31):** svi `sanity/schemas/objects/*.ts` osim infrastrukturnih (`localeString`/`localeText`/`localePortableText`). **`contentBlock.ts` izuzetak** — samo `heading` lokalizovan, `content` (portable text sa custom block-stilovima/marks/ugnježdenim slikama) ostaje neizmenjen jer generički `localePortableText` ne pokriva tu bespoke konfiguraciju bez gubitka mogućnosti, a polje se nigde ne renderuje na frontend-u (orphaned iz originalnog scaffold-a).
  - **Dosledan princip kroz svih 51 fajl:** lična imena (doctor/testimonial/staffList/team/board members `name`) i enum/select vrednosti sa fiksnom listom opcija (news.category, announcement.type, jobPosting.type, video.source) OSTAJU plain `string` — nisu tekst za čitaoca nego identifikatori ili imena ista na oba jezika; kontakt podaci (telefon/email/adresa) svuda NEIZMENJENI. Svaki ugnježdeni `preview.select` u Studio-u ažuriran na `.sr` sufiks gde polje postaje `{sr,en}` objekat (inače bi se Studio lista prikazivala kao `[object Object]`) — ovo lint ne hvata, provereno ručno fajl po fajl.
  - **Watch-item (isti kao Korak 1, i dalje važi):** čisto schema-level izmena — `production` dataset dokumenti i dalje čuvaju stare plain-string vrednosti dok se ne pokrene Faza 3c migraciona skripta; postojeći dokumenti će u Studio-u izgledati prazni na dirana polja dok se ne migriraju.
  - **Sledeći korak:** Faza 3c (`scripts/migrate-i18n-schema.ts`) — vidi `TASKS.md`.
- 2026-08-04 — **i18n Faza 3b Korak 1 (Sanity šema lokalizacija — infrastruktura + POC).** Nastavak i18n taska posle Faze 3a (rutiranje). Novo: 3 reusable lokalizovana object tipa (`sanity/schemas/objects/localeString.ts`/`localeText.ts`/`localePortableText.ts`, `{sr, en}` shape) + deljena Studio input komponenta `sanity/components/LocaleTabInput.tsx` koja renderuje sr/en podpolja kao tabove (deleguje stvarno renderovanje na Sanity-jev `renderField`, samo menja layout) + `sanity/lib/locale.ts` `pick<T>(field, locale)` helper (EN pada nazad na SR dok prevod ne postoji). `@sanity/ui` dodat kao **direktna** zavisnost (`^3.5.1`) — ranije je postojala samo kao tranzitivna zavisnost ugnježdena na 4 različite putanje u `node_modules` (različiti Sanity paketi su je povlačili odvojeno); dodavanje kao top-level dependency ih je `npm install`-om deduplikovalo u jednu kopiju — bitno da izbegnemo dva odvojena React Context-a/theming instance iz dve kopije `@sanity/ui` u istom Studio bundle-u. Proof-of-concept primenjen SAMO na `page.ts` (`title`/`subtitle`, `slug` izvor promenjen na `title.sr`) i `hero.ts` (`heading`/`subheading`) — namerno ograničeno, po planu iz `TASKS.md`, da vlasnik sajta prvo odobri tabbed sr/en UX pre nego što se isti pattern primeni na preostalih ~49 schema fajlova (Korak 2). `npx tsc --noEmit`, `npm run build` (exit 0), `npm run lint` (27 problema, identično baseline-u) — bez regresija.
  - **Watch-item (očekivano, ne bug):** ovo je čisto schema-level izmena — postojeći `page`/`hero` dokumenti u `production` datasetu i dalje čuvaju `title`/`subtitle`/`heading`/`subheading` kao obične stringove (stari format). Frontend (GROQ upiti + TS tipovi) ostaje nepromenjen i i dalje čita te stringove ispravno — ništa nije izgubljeno. Ali u Studio-u će ta polja na postojećim dokumentima izgledati **prazna** dok se ne pokrene migraciona skripta (Faza 3c) koja stare stringove pretvara u `{sr, en}` objekte. Ako urednik ne dira ta konkretna polja pre migracije, stari sadržaj ostaje netaknut (Sanity patch-uje samo dirana polja).
  - **Sledeći korak:** čeka se da vlasnik sajta otvori Studio (`npm run dev`) i potvrdi da mu se sviđa tabbed sr/en layout na title/subtitle/hero poljima, pre nastavka na Korak 2 (pun rollout na ~49 preostalih schema fajlova).
- 2026-08-04 — **Header spojen u jedan segment + Ćир/Lat toggle + i18n Faza 3a (rutiranje).** Tri odvojena rezultata u istoj sesiji:
  - **Header redizajn** (`components/shared/Header/`) — logo + kontakt traka + navigacioni meni spojeni u jedan `headerCard` (bili su dva vizuelno razdvojena bloka). Prva verzija (CSS grid, logo spanning oba reda) je slomila nav meni u 2 reda (logo je oduzimao širinu i navu) — ispravljeno na flex stacked redove (`topRow` = logo+kontakt, `navRow` = puna širina kartice). Verifikovano Playwright screenshotovima (desktop/scrolled/mobile) — nijedan automatizovan browser alat nije bio dostupan u okruženju pa je instaliran Playwright globalno (van `package.json`, po dogovoru sa vlasnikom sajta).
  - **Ćир/Lat transliteracioni toggle → LanguageSwitch** — starei `lib/transliteration/` (mapa + DOM TreeWalker, čuva original tekst u `WeakMap`) ostaje, ali UI je zamenjiv sa `components/shared/LanguageSwitch/LanguageSwitch.tsx` koja nudi tri opcije u jednom kontrolu: "English", "Српски (ћирилица)", "Srpski (latinica)" — samo srpski ima pismo varijantu. Dostupna kao dropdown u desktop headeru i pill-varijanta u mobilnom meniju. Stare komponente `components/shared/LocaleSwitch/` i `components/shared/ScriptToggle/` obrisane.
  - **i18n Faza 3a (rutiranje)** — `next-intl` sa `localePrefix:"as-needed"` (SR bez prefiksa, `/en/*` novo). Svih ~77 ruta premešteno `app/*`→`app/[locale]/*`. Next.js 16 preimenovao `middleware.ts`→`proxy.ts` (deprecation warning, novo ime korišćeno). `build:static` zahtevao workaround skriptu (`scripts/build-static.mjs`, proxy ne radi sa `output:"export"`) + otkrio dublji, nevezan pre-postojeći problem (dinamički Sanity fetch nekompatibilan sa static export) — dokumentovano kao Backlog item, NE rešeno (van scope-a). `npm run build` (Vercel, primarno) potvrđeno čist.
  - **Watch-item:** Faza 3b–3f (Sanity šema lokalizacija, migracija, AI prevod, ožičenje stranica) detaljno razbijeno u `TASKS.md`, nije još započeto — sledeća sesija nastavlja odatle. `npm run lint` nepromenjen baseline (27 problema, sve pre-postojeće, ništa novo iz ovog taska).
- 2026-08-03 — **Kompletirana Sanity migracija** na eksplicitan zahtev vlasnika sajta ("apsolutno sve mora biti na Sanity-ju") — poslednji krug pokrio sve rute koje su do sad ostale hardkodovane ili delom hardkodovane. Pun detalj po stavci u `TASKS.md` sekcija 5 (Aktuelnosti) i sekcija 6 (Header/navigacija, homepage ostatak, O nama, Kontakt); ovde samo sažetak:
  - **Aktuelnosti (9 stranica)** — 5 novih multi-instance document tipova (`video`, `announcement`, `jobPosting`, `magazineIssue`) + `informatorPage` singleton (postojeći `news` dopunjen `fullText` poljem); originalno seed-ovan 32 dokumenta (6 vesti placeholder + 6 gostovanja + 7 obaveštenja + 6 oglasi + 6 časopis + 1 informator). **WordPress migracija 2026-08-05 zamenila 6 placeholder vesti sa 302 članka** sa realnim fotografijama i kategorijama. Sve rute konvertovane u server component + fetch + fallback; teže interaktivne liste (obaveštenja/oglasi/gostovanja/časopis/informator) razdvojene na server wrapper + client komponentu (isti server-shell/client-ostrvo obrazac kao ranije `ProcedureTabs`).
  - **Header/glavna navigacija** — `navigation` singleton (postojao, ali `NAVIGATION_QUERY` se NIGDE nije koristio — orphaned) proširen na 3 nivoa i sad ga stvarno fetch-uje nov `HeaderData.tsx` server wrapper.
  - **Homepage ostatak** — hero slajderi i "Наше клинике"/"За пацијенте" brzi linkovi prešli sa hardkodovanih nizova na 3 nova pageBuilder section tipa.
  - **Kardiohirurgija (20. klinika)** — `clinicPage` schema proširen (ne novi tip) opcionim `stats`/`highlights`/`organizationalStructure`/`units[]` (5 pod-jedinica) poljima; ništa breaking za ostalih 19 klinika.
  - **CardioView3D Lab** — svesno probijena ranija "namerni izuzetak" politika (jedina preostala potpuno bespoke stranica na sajtu) na eksplicitan zahtev; migrirana kao `page` + prošireni `tabsBlock` (novi `introList`/`focusCards`/`outroParagraphs`).
  - **O nama (4 podstranice)** — svih 4 kao `page` dokumenti; nov reusable pageBuilder blok `boardListBlock` (zamenio bespoke `OdboriIOrganiClient.tsx`, obrisana); `cardGridBlock` dobio opciona `contactPerson`/`phone`/`email` polja.
  - **Kontakt + `siteSettings`** — `siteSettings` singleton **nije uopšte postojao u datasetu** (`null` upit) iako ga je kod odavno referencirao — kreiran sa pravim kontakt podacima; `/kontakt` prešao sa `"use client"` na server component (dobio i prvi `export const metadata`, ranije ga uopšte nije imao), kontakt-kartice sad čitaju `siteSettings` umesto duplog hardkoda, forma izdvojena u `KontaktForm.tsx` client ostrvo.
  - Nakon svakog od gornjih koraka posebno provereno `npx tsc --noEmit` (čisto), `npm run build` (exit 0) i `npm run lint`; finalni broj **27 problema — 2 manje od baseline-a 29** (dva `react/no-unescaped-entities` nalaza nestala kao sporedni efekat prelaska teksta iz literalnog JSX-a u Sanity/`data.ts` stringove, u `OdboriIOrganiClient.tsx`/`zdravstvena-akreditacija` koji su u međuvremenu prepisani). Svih 9 novih migracionih skripti potvrđeno upitom na javni Sanity API (ne pretpostavka).
  - **Watch-item:** i dalje ostaje da je dobar deo PRENETOG sadržaja sadržajno netačan/demo/fabrikovan (PDF linkovi na časopis/informator, homepage tim/testimonials/stats) — to je sad editable kroz Studio, ali i dalje čeka pravi materijal/odluku vlasnika sajta, vidi Blokeri iznad.
- 2026-07-23 — Fix: pravi sajt Header/Footer se renderovali preko Sanity Studio-a na `/studio` (i lokalno i na produkciji), sprečavajući klik na prve stavke Studio menija (Почетна/О нама) jer je `Header.module.css` `.mainHeader` `position: fixed; z-index: 2000` preko celog ekrana — otkriveno tokom verifikacije prethodnog taska (Studio meni reorganizacija). Uzrok: `app/layout.tsx` (root layout, primenjuje se na svaku rutu) bezuslovno renderovao `<Header />`/`<Footer />` oko `{children}`, a Next.js App Router ugnježdeni layout-i su aditivni (nested ruta ne može da isključi ancestor layout). Rešenje: novi `components/shared/SiteChrome/SiteChrome.tsx` (`"use client"`, `usePathname()`) uslovno renderuje Header/Footer samo kad pathname NE počinje sa `/studio`; `app/layout.tsx` sada koristi `<SiteChrome>{children}</SiteChrome>` umesto direktnih `<Header />`/`<Footer />` poziva. `npm run lint` bez novih grešaka.
- 2026-07-23 — Reorganizacija Sanity Studio menija: vlasnik sajta prijavio da je default Studio meni potpuno neintuitivan (flat lista svih 16 document tipova, "Stranice" mešala 30 dokumenata iz 3 sekcije sajta bez naznake kojoj pripadaju). Dodat `sanity/structure.ts` (`structureTool({ structure })` u `sanity.config.ts`) koji grupiše meni po redosledu stvarne navigacije sajta (Почетна pinovana direktno → О нама 4 singleton linka → Клинике → За пацијенте/Наука и истраживање/Едукација filtrirano po novom `section` polju → Тим и услуге → Новости и садржај → Навигација/Footer/Подешавања сајта kao flat root stavke). `page` document tip dobio `section` polje (required, `initialValue: "ostalo"`) jer slug sam po sebi ne nosi informaciju o sekciji. Jednokratni `scripts/backfill-page-section.ts` (`npm run migrate:backfill-page-section`) popunio postojećih 30 `page` dokumenata + homepage (0 propuštenih, potvrđeno GROQ `count()` upitima: za-pacijente 12, edukacija 13, nauka-istrazivanje 5 — poklapa se sa `TASKS.md`). Potvrđeno upitom na live dataset da singleton dokumenti (`aboutPage`/`bibliographyPage`/`biographyPage`/`directorPage`/`footer`) imaju fiksan `_id` == naziv tipa (pattern iskorišćen za pinovane linkove); `siteSettings`/`navigation` dokumenti još ne postoje u datasetu (Studio nudi kreiranje pri prvom otvaranju, sajt i dalje koristi hardkodovani fallback). `npm run lint` bez novih grešaka (svi postojeći "any"/warning nalazi su pre-existing, van fajlova dirani u ovom tasku). Vidi `docs/ARHITEKTURA.md` §3.4.
- 2026-07-23 — Sanity migracija sekcije Nauka i istraživanje (5 od 7 stranica): direktan reuse `PAGE_BY_SLUG_QUERY` + infrastrukture iz Za pacijente/Edukacije bez izmena. Migrirano: `centar-izuzetnih-vrednosti`, `saige-projekat`, `aktuelnosti` (slug `nauka-istrazivanje-aktuelnosti`, namerno da izbegne koliziju sa root `/aktuelnosti` sekcijom 5), `lista-istrazivaca` (stara bespoke `ResearchersAccordion` komponenta obrisana — sadržaj se 1:1 uklapa u postojeći `accordionBlock`), `nio` (`/nio/page.tsx` bio `"use client"` → refaktorisan na server, PDF interaktivnost u novu client komponentu `DocumentListBlock`). **Namerni izuzetak:** `cardioview3d-lab` ostaje hardkodovana (3 taba sa specifičnom ugnježdenom strukturom koja se ne ponavlja drugde — po politici ne opravdava custom block, kao `kardiohirurgija` u Klinike). Schema izmene: `cardGridBlock` dobio opciona `date`/`category` polja (za aktuelnosti kartice); **nov** `documentListBlock` reusable block tip (PDF liste, za /aktuelnosti/informator, /aktuelnosti/casopis-dedinje, /akta-instituta u backlogu). `scripts/migrate-nauka-istrazivanje.ts` (`npm run migrate:nauka-istrazivanje`) pokrenut uspešno, svih 5 `page` dokumenata potvrđeno na javni Sanity API. Orphaned `page.module.css` obrisani. `npm run build` (exit 0) i `npm run lint` (29 problema, identično baseline-u) — bez regresija. Sledeće na redu: Aktuelnosti sekcija (sekcija 5).
  - **Watch-item (komponenta obrisana):** `components/shared/ResearchersAccordion/` obrisana jer sadržaj stranice lista-istrazivaca (4 kategorije sa listom istraživača) 1:1 odgovara postojećem `accordionBlock` + `AmbulanteAccordion` rendereru — nema potrebe za posebnom komponentom. Ako se u budućnosti pojavi drugačiji tip akordeona koji ne staje u `accordionBlock`, dodaće se novi block tip, ne vraćati bespoke komponentu.
- 2026-07-21 — Reorganizovana dokumentacija: `docs/` folder (`ARHITEKTURA.md`, `PROJECT_STATUS.md`, `MIGRACIJA.md`, `TASKS.md`), `CLAUDE.md` skraćen na indeks + konvencije, `README.md` skraćen, dodat `docs-updater` subagent i model-forcing hook u `.claude/settings.json`. Stari `MIGRATION_DIRECTOR.md`/`SANITY_SETUP.md` obrisani (sadržaj prenet u `docs/`).
- 2026-07-21 — Unet sav sadržaj iz TASKS.md "Sadržaj za unos" liste (4 brze korekcije + 14 stavki novog sadržaja), direktno u postojeći hardkodovani obrazac (bez Sanity migracije, koja ostaje poseban budući task). Ključne izmene: `ClinicPageTemplate.tsx` proširen sa 3 opciona bloka (`proceduresList`, `staffList`, `patientInstructions`); ažurirane postojeće klinike (vaskularna-hirurgija, invazivna-dijagnostika, kv-dijagnostika, kardiologija, kardiohirurgija + `units.ts`, transfuzija, telemedicina, škola ehokardiografije, interna-edukacija); kreirano 9 novih ruta (`/klinike/elektrofiziologija`, `/klinike/kardiovaskularna-rehabilitacija`, `/klinike/neurokardioloska-laboratorija`, `/klinike/cusmo`, `/klinike/neinvazivna-dijagnostika-srca`, `/za-pacijente/preoperativna-priprema`, `/nauka-istrazivanje/cardioview3d-lab` + redirect sa stare `/workshop` rute, `/edukacija/sestrinska-edukacija` hub + 4 podstranice); Header.tsx meni ažuriran sa novim linkovima. `npm run build` i `npm run lint` prolaze bez novih grešaka (baseline od 14 pre-postojećih lint grešaka nepromenjen).
  - **Watch-item (privatnost):** CUSMO izveštaji sa radnih poseta i konzilijuma namerno uneti samo kao agregatni rezime (broj pacijenata/datumi), ne pojedinačni klinički detalji iz izvornih docx-eva (koji sadrže godinu rođenja, pol, dijagnozu, hirurški opis) — potvrđeno sa vlasnikom sajta tokom rada.
  - **Watch-item (izvori):** dva izvorna fajla u `docs za ubacivanje/` su bila pogrešno atribuirana u prethodnoj verziji TASKS.md — `Provera stranica i novi predlozi/УВОДНА РЕЧ.docx` je sadržaj za Kliniku za kardiohirurgiju (ne rehabilitaciju), a `stranica i novi predlozi/Sajt IKVBD.docx` je sadržaj za Centar za kardiovaskularnu rehabilitaciju (ne kardiohirurgiju) — ispravljeno u TASKS.md.
  - **Preostalo iz "Sadržaj za unos":** 2 ☁️ stavke (spajanje Kardiohirurgija-1/2 u "Odeljenje poluintenzivne nege", reorganizacija `/edukacija`) i dalje čekaju odluku vlasnika sajta, namerno nisu dirane.
- 2026-07-21 — Batch 1 (od 7) popune sadržaja sa starog sajta `ikvbd.org`: upoređene sve "О нама" stranice (rec-direktora, o-institutu, biografija, bibliografija, nemedicinski-poslovi, odbori-i-organi-instituta, zdravstvena-akreditacija, lokacija) sa live sajtom. Rezultat: skoro sve već sadrže pun/ekvivalentan sadržaj (Sanity `aboutPage` migracija je već imala kompletne statistike i management profile; hardkodovane stranice o odborima/nemedicinskim poslovima se poklapaju 1:1 sa live sajtom). Jedina izmena: dodat Факс broj na `/o-nama/lokacija`. Otkrivena nova praznina: `/akta-instituta` ne postoji kod nas (dodato u `TASKS.md` backlog). Ostalih 6 batch-eva (Клинике, За пацијенте, Наука и истраживање, Едукација, Актуелности) su u toku, vidi `docs/TASKS.md` → "Popuna sadržaja sa starog sajta".
- 2026-07-21 — Batch 2 i 3 popune sadržaja sa starog sajta: dopunjeno 10 klinika stranica istorijatom, statistikom, kadrom i uputstvima za pacijente sa live `ikvbd.org` (anesteziologija, centar-srcana-slabost, laboratorija, poliklinika, vaskularna-hirurgija, invazivna-dijagnostika, kv-dijagnostika, telemedicina, kardiologija, kardiohirurgija). `ClinicPageTemplate.tsx` proširen opcionim poljem `introParagraphs` (višepasusni uvodni tekst) za ovu svrhu. Usput ispravljen sintaksni bug (prav navodnik unutar JS stringa) u anesteziologija/page.tsx, uhvaćen lint-om pre commit-a. 3 stranice bez live ekvivalenta preskočene (apteka, edukacija-prevencija, klinicka-patologija), transfuzija već identična live sajtu.
- 2026-07-21 — Batch 4 popune sadržaja: **rešen dugogodišnji blokер** — `app/za-pacijente/kardiologija/page.tsx` je imao 9/10 tabova kao "coming soon" stub; popunjeno pravim sadržajem sa live sajta (PFO/ASD zatvaranje, koronarografija, PCI, spirometrija, TAVI, test opterećenjem, TEE, TTE, farmakološki stres eho), a neiskorišćena `ComingSoon` pomoćna komponenta uklonjena. Popunjeno i 9 praznih pitanja na elektrofizioloske-procedure i elektrostimulativne-procedure stranicama. Ostalih 9 za-pacijente stranica su već bile kompletne, bez izmena.
- 2026-07-21 — Docx-audit sweep: svih 25 docx fajlova iz `docs za ubacivanje/` konvertovano (mammoth) i upoređeno rečenicu po rečenicu sa 18 stranica popunjenih u ranijem "Sadržaj za unos" tasku. 14/18 se poklapa tačno. Ispravljeno: `app/klinike/cusmo/page.tsx` (kapitalizacija МСС/МС, ime "Alessandro Frigol" umesto "Frigo"), `app/nauka-istrazivanje/cardioview3d-lab/page.tsx` (dodat kontakt Miljenko Subašić/Maša Petrović, bez email adresa jer ih docx izvor nije sadržao). Otvoreno u backlogu: kardiohirurgija stat "95,5%" bez izvora, preoperativna priprema moguće nedostajući lek "Хсердоксо" — oba čekaju potvrdu vlasnika sajta. `npm run lint` prošao bez novih grešaka.
- 2026-07-21 — Audit početne strane: potvrđeno preko javnog Sanity API-ja da je homepage najvećim delom neizmenjen demo-seed iz `scripts/migrate-all.ts` (izmišljeni doktori/pacijenti sa stock fotografijama, neproverene statistike). Ispravljen `components/shared/Header/Header.tsx` telefon (668→700, potvrđeno unakrsnom proverom sa 13+ pravih stranica). Footer kontakt takođe ispravljen (668/669→700, info@ikvbd.rs→info@ikvbd.com) — inicijalni patch pokušaj nije uspeo jer je `SANITY_API_TOKEN` bio read-only/Viewer; vlasnik sajta je napravio novi token sa Editor pravima, upisan u `.env.local`, i `scripts/migrate-footer.ts` ažuriran + ponovo pokrenut. Tim/testimonials/stats sekcije namerno ostavljene netaknute na zahtev vlasnika sajta dok se ne obezbedi pravi materijal. Pun nalaz u `popunjene-stranice-2026-07-21.md`.
- 2026-07-21 — Audit + task-breakdown za preostalu Sanity migraciju: potvrđeno da samo 5 stranica (početna, rec-direktora, o-institutu, biografija, bibliografija) zaista fetch-uje sa Sanity-ja, ostalih ~70 (klinike, za-pacijente, edukacija, nauka-istraživanje, aktuelnosti) je potpuno hardkodovano. Dogovorena schema strategija (vidi `MIGRACIJA.md`): bespoke singleton samo za jedinstvene stranice (kao dosad), generički `page` document (page-builder, postojao neiskorišćen) za uniformne info-stranice, i novi multi-instance tipovi (`clinicPage` za ~20 klinika, reuse postojećeg `news` tipa za aktuelnosti) umesto po-jednog-schema-po-stranici. `TASKS.md` dobio detaljan task-breakdown po sekciji, redosled: Klinike → Za pacijente → Edukacija → Nauka-istraživanje → Aktuelnosti. Ovo je bio čisto dokumentacioni task, bez izmena koda/schema.
- 2026-07-21 — Sanity migracija sekcije Klinike (19 od 20 stranica): kreiran multi-instance `clinicPage` document schema (`sanity/schemas/documents/clinicPage.ts`), `CLINIC_PAGE_QUERY`/`CLINICS_LIST_QUERY` u `queries.ts`, `ClinicPage` TS tip u `types.ts`. Sadržaj svake klinike izdvojen iz `page.tsx` u sopstveni `data.ts` (jedan izvor istine za fallback i za seed skriptu). Svih 19 `page.tsx` (sve osim kardiohirurgije, koja ostaje custom sa `units.ts`) konvertovano u server component: Sanity fetch by slug + fallback na `data.ts`. `/klinike` hub sada fetch-uje listu klinika za kartice (kardiohirurgija ostaje hardkodovana prva kartica), sa fallback-om na statičku listu. `scripts/migrate-clinics.ts` (`npm run migrate:clinics`) pokrenut uspešno — svih 19 dokumenata kreirano i potvrđeno upitom na javni Sanity API. `npx tsc --noEmit`, `npm run build` (exit 0) i `npm run lint` (baseline 31 problem, nepromenjeno) prolaze bez regresija. Sledeće na redu: Za pacijente sekcija (vidi `TASKS.md`).
- 2026-07-21 — Sanity migracija sekcije Za pacijente (12 od 12 stranica): građena reusovana infrastruktura za sve 12 stranica različitih oblika sadržaja — 8 novih reusable `pageBuilder` block objekata (`introSection`, `bannerBlock`, `cardGridBlock`, `checklistBlock`, `contactDirectoryBlock`, `accordionBlock`, `faqBlock`, `tabsBlock`) kao strukturovani objekti (ne portable text, jer je izvorni sadržaj bio po stringove/nizove). Dodana `subtitle` polja na `page` document. TS tipovi za sve nove blokove (`IntroSectionBlock`, `BannerBlockData`, `CardGridBlockData`, itd.) i `PatientPage` convenience tip. `PAGE_BY_SLUG_QUERY` ažuriran za `subtitle` (već uključuje `pageBuilder[]` generički, bezbedan za reuse u Edukaciji/Nauci). React komponente (`IntroSection`, `BannerBlock`, `CardGrid`, `ChecklistBlock`, `ContactDirectory`, `ProcedureTabs` kao wrapper oko `SidebarTabs` + `InfoBlock` ekstrakcija iz duplikata, `PageBuilder` renderer) sa `.module.css` po komponenti; `AmbulanteAccordion`/`FaqAccordion` reusovane bez izmena. `scripts/migrate-za-pacijente.ts` (`npm run migrate:za-pacijente`) pokrenut uspešno — svih 12 `page` dokumenata kreirano. Svih 12 `/za-pacijente/<slug>/page.tsx` konvertovano u server component sa Sanity fetch + fallback na `data.ts`. Duplirane CSS klase iz 12 page.module.css fajlova konsolidovane u shared komponente. `npx tsc --noEmit`, `npm run build` (exit 0), `npm run lint` (29 problema, 2 manje nego baseline od 31) — bez regresija. **Watch-item:** infrastruktura je direktno reusovana za Edukaciju i Nauku-istraživanje (pattern se kopira bez izmena). Sledeće na redu: Edukacija sekcija.
- 2026-07-22 — Sanity migracija sekcije Edukacija (16 od 16 stranica — audit tokom rada otkrio 16 stvarnih stranica, ne 12 koliko je ranije procenjeno u `TASKS.md`): 13 stranica reusovalo infrastrukturu iz Za pacijente bez izmena (`page` document + postojećih 8 pageBuilder blokova), uz 2 nova reusable block tipa — `timelineBlock` (wrapuje postojeći `timeline` object, za istorijat/timeline sekcije) i `lectureScheduleBlock` (tabovi po godinama sa predavanjima, reuse `TemePredavaciTabs` komponente za KME raspored predavanja 2017–2020). `cardGridBlock` dobio opciono `href` polje da hub/nav stranice (npr. `/edukacija` sa linkovima ka podstranicama) mogu da se migriraju bez gubitka navigacije. Preostale 3 stranice (škole — ehokardiografija, hipertenzija, vaskularni ultrazvuk) imale kompleksniju strukturu (kursevi sa detaljima, statistike, timovi lekara/tehničara) koja ne staje u generički page builder bez gubitka strukture — dobile bespoke `schoolPage` multi-instance document tip (po uzoru na `clinicPage`) + `SchoolPageTemplate.tsx` komponentu (`app/edukacija/programi/_components/`, konsolidovan CSS iz sve 3 originalne `page.module.css`). `kme-medicinske-sestre-tehnicari/page.tsx` je bio `"use client"` (blokirao Sanity fetch/generateMetadata na server strani) — refaktorisan na server component; interaktivni tabovi ostaju u zasebnoj `TemePredavaciTabs` client komponenti (isti pattern kao `ProcedureTabs`), prateći `layout.tsx` obrisan kao suvišan. `scripts/migrate-edukacija.ts` (`npm run migrate:edukacija`) pokrenut uspešno — 13 `page` + 3 `schoolPage` dokumenata kreirano, svih 16 potvrđeno upitom na javni Sanity API. Orphaned `page.module.css` (16 fajlova) i `app/edukacija/constants.ts` obrisani. `npx tsc --noEmit`, `npm run build` (exit 0, svih 16 ruta dinamičke) i `npm run lint` (29 problema, identično baseline-u) — bez regresija.
  - **Watch-item (pojednostavljenje radi reuse):** `medjunarodni-kongresi` i `radionice` su na starom sajtu imali foto-galerije uz svaku stavku; `cardGridBlock` nema polje za sliku pa su fotografije izostavljene (tekst u potpunosti očuvan). Zabeleženo u `TASKS.md` kao mogući budući follow-up (dodati opciono `image` polje u `cardGridBlock`, slično kako je dodato `href`).
  - Sledeće na redu: Nauka i istraživanje sekcija (7 stranica).
